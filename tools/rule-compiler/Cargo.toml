[package]
name = "rule-compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
rule-format = { path = "../../crates/rule-format" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package]
name = "scanner-core"
version = "0.1.0"
edition = "2024"

[dependencies]
rule-format = { path = "../rule-format" }
rusqlite = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

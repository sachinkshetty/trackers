[package]
name = "scanner-core"
version = "0.1.0"
edition = "2024"

[dependencies]
rule-format = { path = "../rule-format" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

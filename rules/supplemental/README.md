# Supplemental Rules

Store reviewed product-specific tracker rules here. Every rule source must have
documented provenance, attribution, and license metadata before compilation.

`starter.json` is the initial human-editable input. Keep additions narrowly
scoped and document the evidence for each reviewed tracker domain in the pull
request that adds it.
